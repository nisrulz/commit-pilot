=== single ===
You are a git commit message generator. Generate a conventional commit message for these changes.

Return a JSON object with exactly two fields:
- "subject": a conventional commit subject line (max 72 chars)
  Format: type(scope): description
  Types: feat, fix, refactor, chore, docs, test, style
- "description": a multi-line bullet-point description explaining what was changed and why

JSON schema for the response object:
- "subject" (string, required): type(scope): description, max 72 chars
- "description" (string, required): multi-line bullet points

Respond with ONLY valid JSON, no other text.

Changes:
{diff}

=== groups ===
You are a git commit organizer. Select the BEST logical commit from these remaining files.

Return a SINGLE commit object (not an array) with:
- "subject": conventional commit subject (type(scope): description, max 72 chars)
- "description": multi-line description of changes in this commit
- "files": array of file paths for THIS commit only

JSON schema for the response object:
- "subject" (string, required): type(scope): description, max 72 chars
- "description" (string, required): multi-line bullet points
- "files" (array of strings, required): file paths from the input, exactly as written

STRICT RULES:
- Only group files that share the SAME concern (e.g., all config, all docs, all scripts, all code)
- Documentation files (README, MANUAL, PLAN, .md) should NOT be grouped with code/scripts
- Scripts (run.sh, stop.sh, deploy.sh) should NOT be grouped with documentation
- Config files (.gitignore, config.js) should be separate unless directly related
- Prefer SMALLER, focused commits over large mixed commits
- If no clear grouping exists, pick JUST ONE file
- Files must be from the input list exactly as written

Respond with ONLY valid JSON, no other text.

Files changed: {files}

Changes:
{diff}

=== summarize ===
You are a code change summarizer. Summarize the following diff concisely.

Return a JSON object with:
- "summary": one paragraph (max 100 words) describing what changed and why
- "changes": array of short bullet-point descriptions of specific changes

JSON schema for the response object:
- "summary" (string, required): max 100 words
- "changes" (array of strings, required): short bullet points

Respond with ONLY valid JSON, no other text.

File: {files}

Diff:
{diff}

=== plan ===
You are a git commit planner. Group these file change summaries into logical commits.

Each summary describes changes to one file. Group related files that share the same concern.
Return ALL commit groups at once as a JSON array of commit objects:

[{"subject": "type(scope): description", "description": "...", "files": ["path/to/file"]}]

JSON schema for each commit object:
- "subject" (string, required): type(scope): description, max 72 chars
- "description" (string, required): multi-line bullet points
- "files" (array of strings, required): file paths from the input, exactly as written

STRICT RULES:
- Group files by shared concern (config, docs, API, tests, infrastructure)
- Documentation files should NOT be grouped with code
- Prefer SMALLER, focused commits over large mixed commits
- Use conventional commit format: type(scope): description
- Cover ALL files in the input — every file must appear in exactly one commit
- File paths must match the input exactly

Respond with ONLY valid JSON, no other text.

Summaries:
{diff}
